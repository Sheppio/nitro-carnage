/**
 * The game's name, in exactly one place.
 *
 * It is a working title. Everything user-visible — the page title, the menu,
 * the storage keys, the test broker's channel name — derives from these two
 * constants, so renaming the game is a one-line change rather than a search.
 */
export const GAME_NAME = 'NITRO CARNAGE';
/** Machine-safe form: localStorage keys, BroadcastChannel names, topic roots. */
export const SLUG = 'nitrocarnage';
//# sourceMappingURL=brand.js.map